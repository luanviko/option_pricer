# option_pricer
Pricer for vanilla and Asian options


## Installation

All requirements are listed in the `pyproject.toml` 

```
python -m venv .venv_pricer
source .venv_pricer/bin/activate
python -m pip install -e ".[dev]"
```

```
which python pytest
```

